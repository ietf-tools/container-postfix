{{- if .Values.pgsql_relay_domains_mm.create -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.pgsql_relay_domains_mm.name | replace "_" "-" }}
  labels:
    {{- include "postfix.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- with .Values.pgsql_relay_domains_mm.data }}
  {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
